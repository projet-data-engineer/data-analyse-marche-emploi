input {

    elasticsearch {
        hosts => "elasticsearch"
        index => "offres"
        query => '{ "query": { "match_all": {} } }'
    }
}

output {
    file {        
        fields => ["id","romeCode"]
        path => "/export/pole_emploi.csv"
    }
}